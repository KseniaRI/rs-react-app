export interface DataType {
  name: string;
  description: string;
}
export interface ExtractedData {
  name: string;
  description: string;
}

export interface Planet {
  climate: string;
  diameter: string;
  gravity: string;
  name: string;
  terrain: string;
  orbital_period: string;
  population: string;
}
