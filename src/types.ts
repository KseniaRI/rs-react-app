export interface DataType {
  name: string;
  description: string;
}
export interface ExtractedData {
  name: string;
  description: string;
}

export interface Planet {
  name: string;
  terrain: string;
  climate: string;
}
